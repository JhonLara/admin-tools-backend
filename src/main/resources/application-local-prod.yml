spring:
  datasource:
    url: jdbc:postgresql://${***}:${PGPORT:21296}/${PGDATABASE:railway}?sslmode=require
    username: ${PGUSER:postgres}
    password: ${PGPASSWORD:**}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: ${TZ:America/Bogota}

telegram:
  enabled: true
  bot-token: ${TELEGRAM_BOT_TOKEN:8821379353:AAGuz__oVg9GqASXR_AjqtonUY959LtDvlk}
  sales-chat-id: ${TELEGRAM_SALES_CHAT_ID:-5591464960}
  aliados-chat-id: ${TELEGRAM_ALIADOS_CHAT_ID:-5430015388}
  errors-chat-id: -5463513988

logging:
  level:
    com.admintools: DEBUG
    org.hibernate.SQL: DEBUG
